# 週推薦引擎改善計畫（已實作）

> **狀態更新**：本文件原為規劃草稿，已於後續對話中依討論結果全部實作並在瀏覽器驗證通過。
> 實際落地內容：
> - 食譜庫從 36 道擴充到 80 道（早餐15/主食15/肉18/菜16/下午茶10/湯6），新增食譜混入日式、韓式、泰式、墨西哥、地中海等菜系，成本比例約 4:4:2。
> - 湯類別已接入演算法：新增「湯品星期」設定（週推薦頁「餐點規則設定」區塊），兩人各自勾選想喝湯的星期，任一人勾選即會在該天午餐/晚餐加一道湯（熱量佔比主食35%/肉35%/菜15%/湯15%）。
> - 候選1 主食輪替：`Recipe.carb_source`（飯/麵/其他），演算法會避免連續兩天主食用同一種碳水來源。
> - 候選2 黑名單：「餐點規則設定」區塊內，可搜尋食譜加入黑名單，產生/重推菜單一律排除。
> - 候選3 最愛清單：獨立「最愛清單」區塊，軟性加權（評分扣 0.12），比較容易被選到但不保證。
> - 候選4 動態重複上限：候選池 ≥12 道時 `MAX_RECIPE_REUSE` 自動降到 1，食譜庫擴充後自動生效不需手動調整。
> - 候選5（豐盛日/簡單日）未實作，維持原規劃中「排在最後」的建議，之後有需要再談。
>
> 以下為原始規劃草稿內容，保留作歷史紀錄。

---

這份文件是「都做」（食譜庫擴充 + 演算法規則調整）的規劃草稿，先不改程式，供明天討論/修改用。

背景對話：週推薦頁面因為食譜庫太小、規則式演算法缺一些貼近實際習慣的規則，導致推薦結果重複、不夠精準。決定分兩條線處理，互不衝突：

- **食譜庫擴充**：讓演算法有更多、更好的菜可選（一次性資料填充）
- **演算法規則調整**：讓演算法「怎麼選」更聰明（規則會一直生效）

已經先做掉的：[固定餐點功能](backend/meal_plans/models.py)（`FixedMealPreference`）——早餐/下午茶可以固定吃某個食譜，只依熱量調整份量。這份文件討論的是**接下來**要做的部分。

---

## 一、現況盤點

`backend/recipes/seed_data.py` 目前共 **36 道食譜**，依類別分布：

| 類別 | 數量 | 用途 |
|---|---|---|
| 早餐 | 7 | breakfast 單一食譜 |
| 主食 | 7 | lunch/dinner 的 40% 熱量 |
| 肉 | 8 | lunch/dinner 的 40% 熱量 |
| 菜 | 8 | lunch/dinner 的 20% 熱量 |
| 下午茶 | 4 | afternoon_snack 單一食譜 |
| 湯 | 2 | 目前演算法沒用到（沒有對應 meal_type/category 邏輯） |

演算法既有的防重複機制（`selection_algorithm.py`）：
- `MAX_RECIPE_REUSE = 2`：同一份食譜在整週統計裡用滿 2 次會被降權（不是硬性排除，候選池只剩它時還是會選）
- 「昨天吃過」扣分 0.15，不是硬性排除
- 低成本 vs 中高成本比例平衡（低成本要 ≥ 中高成本數量）

以「肉」類只有 8 道來看，兩人 7 天午餐+晚餐要用到「肉」的次數是 14 次，扣掉重複懲罰之後實際上很容易繞回同一批菜，這是「感覺一直吃差不多的東西」的主因。

---

## 二、食譜庫擴充

### 目標數量（草案，待確認）

| 類別 | 現有 | 建議補到 | 備註 |
|---|---|---|---|
| 早餐 | 7 | 15 | 一週 7 天不重複還有餘裕 |
| 主食 | 7 | 15 | |
| 肉 | 8 | 18 | 重複率最高的類別，優先補 |
| 菜 | 8 | 16 | |
| 下午茶 | 4 | 10 | |
| 湯 | 2 | 沿用或砍掉 | 目前沒接進演算法，見下方「順便處理」 |

新食譜會照現有格式寫（`recipe_name, category, base_weight_g, cost_level, is_vegetarian, allergen_tags, ingredients, steps`），營養素用現有 `ingredients` 資料庫的營養值加總算，跟既有食譜的做法一致。

### 需要你明天確認的事

1. **口味/菜系偏好**：現有食譜是台式家常為主（蒜香雞胸排、麻婆豆腐、清炒花椰菜這類）。新增的要延續這個路線，還是也要混一些其他菜系（例如日式、簡易西式）？
2. **成本標籤怎麼分配**：現有「低/中/高」怎麼定義的沒有寫死規則（看起來是主觀標的）。新食譜要不要抓一個大致比例（例如低:中:高 = 5:3:2）？
3. **過敏原/素食標記**：`allergen_tags`、`is_vegetarian` 要不要也一併多樣化（目前素食選項似乎很少），還是暫時都比照現有食譜的分布？
4. **「湯」類別怎麼處理**：要嘛把它接進 `LUNCH_DINNER_CATEGORIES`（變成第四道菜的角色，要重新設計熱量佔比），要嘛先維持現狀不處理、甚至砍掉這兩筆用不到的資料。

---

## 三、演算法規則調整

以下是候選規則，依「已知痛點」排的順序，不是全部都要做——明天挑要做哪幾項、順序怎麼排。

### 候選 1：主食輪替（避免連續兩天同一種澱粉來源）
現在的「昨天吃過扣分」是看整份食譜（例如「雞胸炒飯」），不是看食材類型。如果想避免「連續兩天都是飯類、都沒吃到麵」，需要新增一層「主食的碳水來源」標記（飯/麵/藜麥/其他），再對這個維度做輪替。
**複雜度**：中；需要新增食譜的一個屬性欄位。

### 候選 2：黑名單／不要再出現
讓使用者對某道菜按「不要再推薦」，往後演算法直接排除，而不是只降權。跟「固定餐點」正好相反的機制。
**複雜度**：低；資料結構可以比照 `FixedMealPreference` 做一張 `ExcludedRecipe` 表。

### 候選 3：最愛清單（軟性提高權重，跟「固定」不同）
「固定餐點」是鎖死不變；「最愛清單」則是「常常想吃，但不用每次都吃」，在 `select_recipe_for_slot` 的評分裡加分而不是直接指定。可套用在午餐/晚餐（固定餐點做不到的地方）。
**複雜度**：中；要改評分公式，還要設計「最愛」的資料結構跟前端 UI。

### 候選 4：重複次數上限依食譜庫大小動態調整
`MAX_RECIPE_REUSE = 2` 是寫死的常數。食譜庫變大之後，這個數字可以調低（例如降到 1，因為候選變多了，不需要靠「反正吃完 2 次都還能選」來撐候選池）。
**複雜度**：低；食譜庫擴充完之後重新調參即可，不需要新邏輯。

### 候選 5：週期性豐盛日／簡單日
例如週五、週六晚餐自動挑「中高成本」的菜（犒賞自己），平日維持低成本為主。目前的低成本平衡機制是全週攤平算，沒有「哪幾天特別豐盛」的概念。
**複雜度**：中；牽涉到 `cost_counter` 邏輯要拆成「當週」跟「當天目標」兩層。

### 需要你明天確認的事

1. 上面 5 個候選規則，想做哪幾個？優先順序？
2. 候選 1（主食輪替）需要幫每道「主食」食譜多標一個屬性，要不要一起在食譜庫擴充時處理？
3. 候選 3（最愛清單）需要前端也做對應 UI（跟「固定餐點」放一起，或獨立一個區塊）？

---

## 四、建議的執行順序（草案）

1. 食譜庫擴充（候選 1 的碳水屬性標記如果要做，趁這次一起補）
2. 依食譜庫新規模，重新調整 `MAX_RECIPE_REUSE`（候選 4，幾乎不花力氣，食譜庫擴充完直接做）
3. 依明天確認的優先順序，做規則候選 1-3 中確認要做的項目
4. 候選 5（豐盛日/簡單日）如果要做，因為牽涉既有邏輯改動較大，建議排在最後、單獨驗證

---

*這份文件是規劃草稿，數字（目標食譜數量、成本比例等）都是初步估計，明天討論後會調整再開始動工。*
